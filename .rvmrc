
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p299@mmjmenu" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.8.7-p299@mmjmenu"
else
  rvm --create use  "ruby-1.8.7-p299@mmjmenu"
fi
